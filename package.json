{
    "name": "hubspot_provider",
    "scripts": {
        "update": "pulumi up --skip-preview -p 8",
        "destroy": "pulumi destroy --skip-preview -p 8",
        "import_checkpoint": "pulumi stack export > checkpoint.json",
        "export_checkpoint": "cat checkpoint.json | pulumi stack import"
    },
    "devDependencies": {
        "@types/node": "^13.7.7"
    },
    "dependencies": {
        "@pulumi/pulumi": "^1.0.0",
        "axios": "^0.21.2",
        "chai": "^4.2.0"
    }
}
